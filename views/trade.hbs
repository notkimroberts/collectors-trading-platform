<h3>Trade Matches</h3>
<h7><a href="/collector/trade/images/{{{otherUser.collector_id}}}">view in image list format</a></h7>
<div class="row">
    <div class="column">
        <h4>Collectibles you have that <a href="/collector/list/{{{otherUser.collector_id}}}">{{{otherUser.username}}}</a> wants:</h4>
        {{#if wantsPublic}}
        {{#if currentUserMatchesExist}}
        <table class="table table-striped">
            <thead>
                <th>Collectible</th>
                <th>Type</th>
                <th>Qty To Trade</th>
            </thead>
            <tbody>
                {{#each currentUserCollectibles}}
                    <tr>
                        <td style="word-break: break-word;" width="200px"><a href="/collectible/{{{collectible_id}}}">{{{name}}}</a></td>
                        <td width="150px">{{{typeName}}}</td>
                        <td width="130px">{{{willing_to_trade_quantity}}}</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
        {{else}}
        <p>No matches</p>
        {{/if}}
        {{else}}
        <p>This collector's want list is private</p>
        {{/if}}
    </div>

    <div class="column">
        <h4>Collectibles <a href="/collector/list/{{{otherUser.collector_id}}}">{{{otherUser.username}}}</a> has that you want:</h4>
        {{#if tradesPublic}}
        {{#if otherUserMatchesExist}}
        <table class="table table-striped">
            <thead>
                <th>Collectible</th>
                <th>Type</th>
                <th>Qty To Trade</th>
            </thead>
            <tbody>
                {{#each otherUserCollectibles}}
                    <tr>
                        <td style="word-break: break-word;" width="200px"><a href="/collectible/{{{collectible_id}}}">{{{name}}}</a></td>
                        <td width="150px">{{{typeName}}}</td>                    
                        <td width="130px">{{{willing_to_trade_quantity}}}</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
        {{else}}
        <p>No matches</p>
        {{/if}}
        {{else}}
        <p>This collector's trade list is private</p>
        {{/if}}
    </div>
</div>
<link rel="stylesheet" href="/css/trade.css"/>